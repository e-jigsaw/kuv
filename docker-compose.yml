services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: picsur
      POSTGRES_USER: picsur
      POSTGRES_PASSWORD: picsur
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U picsur -d picsur"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - db-data:/var/lib/postgresql/data

  api:
    build:
      context: .
      dockerfile: packages/api/Dockerfile
    environment:
      PORT: 3001
      PICSUR_JWT_SECRET: ${PICSUR_JWT_SECRET}
      PICSUR_DB_HOST: postgres
      PICSUR_DB_USER: picsur
      PICSUR_DB_PASSWORD: picsur
      PICSUR_DB_DATABASE: picsur
    depends_on:
      postgres:
        condition: service_healthy

  caddy:
    image: caddy:2-alpine
    ports:
      - "8080:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      # web は静的SPA。`pnpm --filter @picsur/web build` で生成した dist/client を bind-mount する。
      # `docker compose up` の前に web をビルドしておくこと。
      # （本番向けに web をイメージへ焼く self-contained 化は後続のデプロイ整備フェーズで対応）
      - ./packages/web/dist/client:/srv/web
    depends_on:
      - api

volumes:
  db-data:
